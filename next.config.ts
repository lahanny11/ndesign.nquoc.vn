import type { NextConfig } from 'next'

const nextConfig: NextConfig = {
  // NDesign — internal design order management tool
  // Desktop-first dashboard, documented in ADR-001
}

export default nextConfig
